version: "3.9"

services:
  app:
    build: .
    container_name: photo-collection
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
    restart: unless-stopped